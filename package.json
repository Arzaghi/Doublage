{
  "name": "doublage",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the Doublage Chrome extension (signed CRX3 packaging).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:crx": "node scripts/build.mjs",
    "test:gemini": "node --test tests/gemini-integration.test.mjs"
  },
  "devDependencies": {
    "crx3": "^2.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}